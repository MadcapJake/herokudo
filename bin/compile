#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

set -e
set -o pipefail   # don't ignore exit codes when piping output

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found a META6.json"

BUILD_DIR=${1:-}
CACHE_DIR=${2:-}
# ENV_DIR=${3:-}
# BP_DIR=$(cd $(dirname ${0:-}); cd ..; pwd)
RAKUDOBREW_BIN=$BUILD_DIR/.rakudobrew/bin
SITE_BIN=$BUILD_DIR/.rakudobrew/moar-nom/install/share/perl6/site/bin

echo "-----> Cloning rakudobrew repo..."
git clone https://github.com/tadzik/rakudobrew $BUILD_DIR/.rakudobrew -q
echo "cloning completed" | indent

export PATH=$RAKUDOBREW_BIN:$PATH

echo "-----> Initializing rakudobrew..."
rakudobrew init 2>&1 /dev/null
echo "rakudobrew initialized" | indent

echo "-----> Building moar..."
rakudobrew build moar 2>&1 /dev/null
echo "moar built" | indent

echo "-----> Building panda..."
rakudobrew build panda 2>&1 /dev/null
echo "panda built" | indent

export PATH=$SITE_BIN:$PATH

echo "-----> Installing Task::Star..."
panda install Task::Star 2>&1 /dev/null
echo "Task::Star modules installed" | indent

mkdir -p $BUILD_DIR/.profile.d
echo "PATH=$RAKUDOBREW_BIN:$SITE_BIN:$PATH" > $BUILD_DIR/.profile.d/perl6.sh
